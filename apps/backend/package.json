{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "air || go run .",
    "build": "go build -o bin/server",
    "start": "./bin/server",
    "migrate": "make migrate-up",
    "sqlc": "make sqlc"
  }
}
